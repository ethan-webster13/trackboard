/**
 * Pipeline — the Kanban board of saved job applications.
 * Placeholder for now; built out in Phases 5 & 6.
 */
export default function Pipeline() {
  return (
    <section>
      <h1>My Pipeline</h1>
      <p style={{ color: 'var(--color-text-muted)', marginTop: 'var(--space-2)' }}>
        Your saved applications, organized as a Kanban board, will live here.
      </p>
    </section>
  )
}
