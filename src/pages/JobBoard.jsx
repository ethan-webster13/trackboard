/**
 * JobBoard — the live job listings page.
 * For now it's a placeholder; in Phase 3 we'll fetch real jobs from the
 * Remotive API and render them here.
 */
export default function JobBoard() {
  return (
    <section>
      <h1>Job Board</h1>
      <p style={{ color: 'var(--color-text-muted)', marginTop: 'var(--space-2)' }}>
        Live remote job listings will appear here.
      </p>
    </section>
  )
}
