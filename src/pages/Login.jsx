/**
 * Login — sign in / sign up page.
 * Placeholder for now; wired to Firebase Auth in Phase 4.
 */
export default function Login() {
  return (
    <section>
      <h1>Log in</h1>
      <p style={{ color: 'var(--color-text-muted)', marginTop: 'var(--space-2)' }}>
        Authentication with Firebase comes in a later step.
      </p>
    </section>
  )
}
